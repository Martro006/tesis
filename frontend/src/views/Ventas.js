import { Button, ButtonGroup, Card, CardTitle } from "reactstrap";

const Ventas = () => {
    const character = {
        top: "200px",
        position: "relative"
    };

    return (
        <Card style={character}>
            <Button>Hola</Button>
        </Card>
    );
}

export default Ventas;